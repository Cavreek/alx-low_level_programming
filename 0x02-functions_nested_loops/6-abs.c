#include "main.h"

/**
  * _abs - Computes the absolute value of an integer
  * @int: The integer to be compute into an absolute value
  *
  * Return: Returns o
  */
int _abs(int)
{
	if (int al >= 0)
	{
		int bl = -al;

		return (bl);
	}
	else
		return (al);
	_putchar('\n');
}
