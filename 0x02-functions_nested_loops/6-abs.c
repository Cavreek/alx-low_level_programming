#include "main.h"

/**
  * _abs - Computes the absolute value of an integer
  * @al: The integer to be compute into an absolute value
  *
  * Return: Returns o
  */
int _abs(int al)
{
	if (al >= 0)
	{
		return (al);
	}
	else
	{
		return (al * -1);
	}
